resources:
    - name: repo_docker_build_push
      type: GitRepo
      configuration:
        gitProvider: jvalance_github                                     # replace with your integration
        path: valancej/jfrog-pipelines       # replace with your repository name
        branches:
          include: master